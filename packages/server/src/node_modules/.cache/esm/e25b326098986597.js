let gql;_989‍.x([["typeDefs",()=>typeDefs],["resolvers",()=>resolvers]]);_989‍.w("apollo-server-express",[["gql",["gql"],function(v){gql=v}]]);

       const typeDefs = gql`
  type Demand {
    id: ID!
    name: String!
    client: Client
    deadline: String
  }

  extend type Query {
    demands: [Demand]!
  }
`;

       const resolvers = {
  Query: {
    demands: async () => {},
  },
};
